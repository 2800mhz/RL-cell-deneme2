# Base runtime dependencies for the desktop simulator.
# The project itself uses only the Python standard library for the core UI/simulation.
# Install this file when you want the lightweight local workflow.

